<Window x:Class="WarframeM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarframeM"
        mc:Ignorable="d"
        Title="Warframe Market 查询" 
        Height="600" Width="1000"
        MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="14"
        Background="#FAFAFA">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部概览区域 -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 单个卡片，内容显示在左右两侧 -->
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 包含两个信息的卡片 -->
                <Border Grid.Column="0" Style="{StaticResource OverviewCardStyle}">
                    <!-- 使用Grid实现左右分布 -->
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- 左侧信息（左对齐） -->
                        <StackPanel Grid.Column="0" HorizontalAlignment="Left">
                            <TextBlock Text="最低价" Style="{StaticResource CardTitleStyle}" FontSize="40"/>
                            <TextBlock x:Name="lowestPriceCard" Text="-" Style="{StaticResource CardValueStyle}" FontSize="40"/>
                        </StackPanel>

                        <!-- 右侧信息（右对齐） -->
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <TextBlock Text="平均价格" Style="{StaticResource CardTitleStyle}" FontSize="40"/>
                            <TextBlock x:Name="averagePriceCard" Text="-" Style="{StaticResource CardValueStyle}" FontSize="40"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- 右侧快捷入口区域 -->
            <StackPanel Grid.Column="1" Width="300" Margin="15,0,0,0">
                <!-- 商品名称卡片 -->
                <Border Style="{StaticResource QuickCardStyle}">
                    <StackPanel>
                        <TextBlock Text="商品名称" Margin="0,0,0,5" Foreground="{StaticResource TextSecondaryColor}"/>
                        <TextBox x:Name="itemNameTextBox"/>
                    </StackPanel>
                </Border>

                <!-- 操作按钮卡片 -->
                <Border Style="{StaticResource QuickCardStyle}">
                    <StackPanel>
                        <Button x:Name="searchButton" Content="查询" Click="SearchButton_Click"
                                Margin="0,0,0,5"/>
                        <Button x:Name="clearButton" Content="清除" Click="ClearButton_Click"
                                Margin="0,0,0,5"/>
                        <Button x:Name="notifyButton" Content="开启价格通知" Click="NotifyButton_Click"/>
                        <TextBlock>开启价格通知后才能选择时间</TextBlock>

                        <!-- 通知间隔选择器 -->
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="间隔:" VerticalAlignment="Center" Margin="0,0,5,0" 
                                       Foreground="{StaticResource TextSecondaryColor}"/>
                            <ComboBox x:Name="intervalComboBox" Grid.Column="1"
                                      SelectionChanged="IntervalComboBox_SelectionChanged"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- 状态信息 -->
        <Border Grid.Row="1" Background="#FFF1F8E9" CornerRadius="8" 
                Padding="10,6" Margin="0,0,0,15"
                x:Name="statusBorder">
            <TextBlock x:Name="statusText" Foreground="#33691E" TextWrapping="Wrap"/>
        </Border>

        <!-- 卖家信息列表 -->
        <DataGrid x:Name="ordersDataGrid" Grid.Row="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="玩家" Binding="{Binding UserName}" Width="150"/>
                <DataGridTextColumn Header="数量" Binding="{Binding Quantity}" Width="60"/>
                <DataGridTextColumn Header="等级" Binding="{Binding ModRankDisplay}" Width="60"/>
                <DataGridTextColumn Header="价格" Binding="{Binding Platinum}" Width="80"/>
                <DataGridTextColumn Header="状态" Binding="{Binding UserStatus}" Width="80"/>
                <DataGridTextColumn Header="平台" Binding="{Binding Platform}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>